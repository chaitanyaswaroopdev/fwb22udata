extends layout

block content
  h1= title
  - const restaurant = ['restaurant_type','duration','cost']
  - let results;
  script.
    function buy(x){
      alert('purchased item ' + x)
   } 
  div(class='container-fluid')
    div(class='row')
      each results, index in restaurant
        div(class='col-sm-2' onclick='buy('+index+')') #{results} 
        

  
