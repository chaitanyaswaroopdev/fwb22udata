extends layout

block content
  h1= title
  p Welcome to the WebApps Assignment-10
  button(type="button", class="btn btn-secondary")= 'Press Me'
  div(id="target") text of Display 
  script.
    var client_count = 0;
    function update(){
      client_count +=1;
      document.getElementById("target").innerHTML = "Value now " + client_count;
    }
  a(href='gas') Gas
